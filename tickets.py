# Билеты 1–45 (инструменты разработки ПО).
# Каждый элемент — полный текст с заголовком «🎫 Билет N».

TICKETS: list[str] = [
    """
🎫 Билет 1

1. Инструментальные средства разработки ПО --- программы, помогающие создавать, отлаживать, тестировать и сопровождать программные продукты. Классификация: среды разработки (IDE), компиляторы/интерпретаторы, отладчики, системы сборки, системы контроля версий, средства тестирования, анализаторы кода.

2. IDE объединяет в одном интерфейсе редактор кода, отладчик, средства сборки и запуска --- это ускоряет разработку, снижает число ошибок и упрощает навигацию по проекту.

Пример: Visual Studio Code: левая панель --- обозреватель файлов, контроль версий, расширения; центр --- редактор с подсветкой синтаксиса и автодополнением; нижняя панель --- терминал, отладчик, вывод; команда Run позволяет скомпилировать/запустить; встроенный отладчик --- точки останова и просмотр переменных.
    """,

    """
🎫 Билет 2

1. Компоненты IDE: редактор кода (подсветка синтаксиса, автодополнение); отладчик (точки останова, пошаговое выполнение); средства сборки (компиляция, запуск); система подсказок/IntelliSense (контекстные предложения); навигатор проекта.

2. IDE vs текстовый редактор: IDE даёт автодополнение, рефакторинг, встроенную отладку, интеграцию с VCS и системами сборки; текстовый редактор --- только редактирование без глубокой поддержки языка.

Пример: VS Code vs IntelliJ IDEA: языки --- VS Code поддерживает любые через расширения, IntelliJ IDEA заточен под Java/Kotlin, глубокая поддержка JVM; плагины --- VS Code: Marketplace с тысячами бесплатных плагинов, IntelliJ: JetBrains Marketplace, часть платная; отладка --- VS Code: гибкая через launch.json, IntelliJ: мощная встроенная с профилировщиком.
    """,

    """
🎫 Билет 3

1. Система сборки --- инструмент, автоматизирующий преобразование исходного кода в готовый исполняемый продукт: компиляцию, линковку, тестирование, упаковку.

2. Примеры: Maven и Gradle (Java/Kotlin) --- управление зависимостями и жизненным циклом; MSBuild (.NET) --- сборка проектов Microsoft; Make (C/C++) --- классический инструмент на основе Makefile; npm scripts (JavaScript) --- скрипты в package.json.

Пример: Учебный проект (Java/Gradle): 1) compileJava --- компиляция .java в .class; 2) test --- запуск JUnit-тестов, отчёт о результатах; 3) jar --- упаковка в исполняемый .jar-файл. Шаги запускаются командой gradle build одной командой в терминале.
    """,

    """
🎫 Билет 4

1. Компилятор --- переводит весь исходный код в машинный заранее (C, C++, Java); интерпретатор --- выполняет код строка за строкой без предварительной трансляции (Python, JavaScript в браузере).

2. Этапы компиляции: лексический анализ (разбивка на токены) → синтаксический анализ (построение дерева разбора) → семантический анализ (проверка типов, объявлений) → генерация промежуточного кода → оптимизация → генерация машинного кода.

Пример: Фрагмент: int x = "hello"; --- лексический: токены разобраны успешно; синтаксический: структура объявления верна; семантический: ошибка --- несовместимость типов int и String. Фрагмент: if (x = 5) --- синтаксически верно, семантически: предупреждение о присваивании вместо сравнения.
    """,

    """
🎫 Билет 5

1. Отладчик --- инструмент для пошагового выполнения программы, просмотра состояния переменных и выявления логических ошибок в реальном времени.

2. Точка останова (breakpoint) --- место, где выполнение паузируется; пошаговое выполнение: Step Into (войти в функцию), Step Over (перешагнуть), Step Out (выйти); просмотр переменных --- панель Watch/Variables показывает текущие значения.

Пример: Алгоритм суммирования: поставить точку останова на начало цикла; запустить отладку; на каждом шаге смотреть значение счётчика i и накопленной суммы; если на итерации 5 сумма не увеличилась --- значит условие цикла или операция сложения содержит ошибку (например, sum = i вместо sum += i).
    """,

    """
🎫 Билет 6

1. Статический анализ --- проверка кода без его запуска. Цели: выявить ошибки стиля, потенциальные баги, нарушения правил безопасности на ранней стадии, снизить стоимость исправления дефектов.

2. Инструменты: ESLint (JavaScript), Pylint/Flake8 (Python), Checkstyle (Java), SonarQube (многоязыковой анализ качества), PMD (Java), FindBugs/SpotBugs.

Пример: Типичные ошибки в учебном проекте: неиспользуемые переменные; отсутствие проверки на null; слишком длинные методы (>30 строк); дублирующийся код; неправильные отступы; магические числа вместо констант; отсутствие обработки исключений; переменные с нечитаемыми именами (a, x, tmp).
    """,

    """
🎫 Билет 7

1. Динамический анализ --- исследование программы во время её выполнения. Профилирование --- измерение потребления ресурсов (процессор, память, время выполнения методов) с целью оптимизации.

2. Показатели: время выполнения каждого метода (CPU time); использование оперативной памяти (heap); количество вызовов функции; загрузка потоков; количество операций ввода-вывода.

Пример: Программа обработки матриц тормозит: подключить профилировщик (VisualVM, YourKit, cProfile); запустить программу; открыть отчёт --- список методов по убыванию времени выполнения; найти метод, занимающий 80% времени (например, вложенный цикл O(n³)); переписать алгоритм или применить кэширование.
    """,

    """
🎫 Билет 8

1. СКВ --- система хранения истории изменений кода, обеспечивающая совместную работу, откат к предыдущим версиям и отслеживание авторства правок.

2. Централизованные (CVS, SVN) --- единый сервер, участники работают напрямую с ним; распределённые (Git, Mercurial) --- каждый разработчик имеет полную копию репозитория, работает локально, синхронизирует с сервером при необходимости.

Пример: git init (создать локальный репозиторий) → создать файл Main.java → git add Main.java (добавить в индекс) → git commit -m "Первый коммит" (зафиксировать) → git remote add origin <url> → git push origin main (отправить на сервер).
    """,

    """
🎫 Билет 9

1. Ветвление позволяет вести параллельную разработку разных функций без влияния на основную ветку; слияние (merge) объединяет изменения из ветки в целевую.

2. Конфликт слияния возникает, когда два разработчика изменили одну строку по-разному. Разрешение: Git помечает конфликт в файле (<<<< ==== >>>>); разработчик вручную выбирает нужный вариант; сохраняет и делает git add + git commit.

Пример: git checkout -b feature/login (создать ветку) → внести изменения → git add . → git commit -m "Добавлена авторизация" → git checkout main → git pull origin main (обновить main) → git merge feature/login (слить) → при конфликтах: исправить вручную → git add . → git commit → git push origin main.
    """,

    """
🎫 Билет 10

1. Issue-трекер (Jira, GitLab Issues, GitHub Issues) --- система учёта задач, ошибок и улучшений. Помогает планировать работу, назначать исполнителей и отслеживать прогресс.

2. Связь: в сообщении коммита указывают номер задачи (например, #15); трекер автоматически закрывает задачу при мерже ветки; релиз формируется из закрытых задач версии.

Пример: Задачи: #1 Создать форму авторизации; #2 Подключить базу данных; #3 Реализовать поиск студентов; #4 Написать юнит-тесты; #5 Оформить README. Связь с репозиторием: git commit -m "fix #3: исправлен регистр в поиске" --- задача #3 обновит статус автоматически.
    """,

    """
🎫 Билет 11

1. Сниппет --- шаблон часто используемого фрагмента кода, вставляемый по короткому ключевому слову; шаблон (template) --- заготовка файла или класса с базовой структурой.

2. Автодополнение (IntelliSense/Autocomplete) предлагает варианты продолжения кода по контексту: имена методов, классов, параметры. Ускоряет набор, снижает количество опечаток, помогает изучать API.

Пример: Сниппеты (Java): 1) sout → System.out.println(""); 2) fori → for (int i = 0; i < n; i++) { } 3) psvm → public static void main(String[] args) { } --- вводим ключевое слово, нажимаем Tab --- IDE разворачивает полный блок.
    """,

    """
🎫 Билет 12

1. Средства работы с БД в IDE: встроенные клиенты (Database Tool в IntelliJ IDEA, SQLiteViewer в VS Code); графические клиенты (DBeaver, pgAdmin, MySQL Workbench); плагины для просмотра таблиц, выполнения запросов, визуализации схемы.

2. Подключение к СУБД: указать драйвер (JDBC/ODBC), хост и порт сервера, имя базы данных, логин и пароль; IDE проверяет соединение и отображает структуру таблиц.

Пример: В IntelliJ IDEA: Database → + → Data Source → PostgreSQL → заполнить host=localhost, port=5432, database=students, user=admin, password → Test Connection → OK. Запросы: SELECT * FROM students WHERE group_id=1; INSERT INTO students(name, group_id) VALUES('Иванов', 1); UPDATE students SET group_id=2 WHERE id=5;
    """,

    """
🎫 Билет 13

1. ER-диаграмма (Entity-Relationship) --- графическое представление сущностей БД и связей между ними. Инструменты: dbdiagram.io, draw.io, MySQL Workbench, DataGrip, ERwin.

2. Связь моделей и кода: каждая сущность ER-диаграммы соответствует таблице в БД и классу/сущности в коде (ORM). Атрибуты → поля класса; связи → внешние ключи и ссылки между объектами.

Пример: Сущности: Student (id, ФИО, дата рождения) и Group (id, название, куратор). Связь: Student принадлежит одной Group (многие-к-одному), поле group_id в таблице students. В коде: класс Student имеет поле Group group; при запросе ORM автоматически подставляет объект Group.
    """,

    """
🎫 Билет 14

1. UML-редакторы --- средства создания диаграмм UML (StarUML, PlantUML, Visual Paradigm, Lucidchart, draw.io); CASE-средства --- комплексные инструменты автоматизации проектирования (Enterprise Architect, Rational Rose).

2. Преимущества CASE: генерация каркаса кода из диаграмм, обратное проектирование (код → диаграмма), единая база моделей для команды, проверка согласованности архитектуры.

Пример: В StarUML: File → New Project → добавить Class Diagram; перетащить Class из палитры → задать имя Student; добавить атрибуты (id: int, name: String); добавить второй класс Group; провести связь «многие-к-одному»; результат --- визуальная схема, которую можно экспортировать в PNG или генерировать SQL-скрипт.
    """,

    """
🎫 Билет 15

1. Управление зависимостями --- автоматическая загрузка и обновление сторонних библиотек, необходимых проекту, с указанием конкретных версий.

2. Репозитории пакетов: Maven Central и JCenter (Java), PyPI (Python), npm Registry (JavaScript), NuGet (.NET) --- централизованные хранилища библиотек, из которых система сборки автоматически скачивает нужные версии.

Пример: Конфигурационный файл (например, build.gradle): раздел «информация о проекте» (название, версия, группа); раздел «зависимости» (основные библиотеки для работы; тестовые библиотеки; библиотека подключения к БД); раздел «задачи сборки» (компиляция, тесты, упаковка в jar).
    """,

    """
🎫 Билет 16

1. Фреймворки модульного тестирования: JUnit (Java), pytest (Python), NUnit (.NET), Jest (JavaScript) --- предоставляют аннотации для тестов, методы утверждений (assert) и средства запуска.

2. Роль автотестов: раннее обнаружение регрессий после изменений; уверенность при рефакторинге; документирование ожидаемого поведения; ускорение проверки по сравнению с ручным тестированием.

Пример: Функция calcAverage(grades): Тест 1 --- calcAverage([4, 5, 5]) → ожидаем 4.67; Тест 2 --- calcAverage([2]) → ожидаем 2.0; Тест 3 --- calcAverage([]) → ожидаем 0 или исключение. Структура (JUnit): @Test аннотация, вызов метода, assertEquals(ожидаемое, фактическое).
    """,

    """
🎫 Билет 17

1. Инструменты интеграционного тестирования: Testcontainers (реальные БД в контейнерах), REST Assured (API), Selenium WebDriver (UI); системного: Selenium, Cypress, Robot Framework.

2. Тестовая среда --- изолированное окружение, воспроизводящее условия продакшена: тестовая БД, заглушки внешних сервисов (stubs/mocks), отдельный сервер. Инструменты: Docker для поднятия сервисов, Mockito/WireMock для имитации зависимостей.

Пример: Сценарий: студент добавляется через модуль регистрации и появляется в списке группы. Инструменты: JUnit + Spring Boot Test для запуска контекста; Testcontainers --- реальная PostgreSQL в Docker; REST Assured --- HTTP-запрос POST /students; затем GET /groups/1/students и проверка через assertEquals.
    """,

    """
🎫 Билет 18

1. Непрерывная интеграция (CI) --- практика автоматической сборки и проверки кода при каждом коммите в репозиторий, чтобы выявлять ошибки как можно раньше.

2. Этапы CI-пайплайна: 1) Получение кода из репозитория; 2) Сборка (компиляция); 3) Запуск автоматических тестов; 4) Статический анализ кода; 5) Формирование отчёта и уведомление команды.

Пример: При каждом git push в GitLab CI: 1) stage: build --- mvn compile; 2) stage: test --- mvn test (запуск JUnit-тестов, сохранение отчёта); 3) stage: lint --- запуск Checkstyle; если хотя бы один шаг упал --- статус «Failed», в Telegram/email уходит уведомление.
    """,

    """
🎫 Билет 19

1. Непрерывная доставка (CD) --- автоматическая подготовка релиза до состояния, готового к развёртыванию; непрерывное развёртывание --- автоматический деплой без ручного подтверждения.

2. CI vs CD: CI --- автоматическая сборка и тестирование после коммита; CD --- автоматическая доставка/деплой прошедшего тесты артефакта на целевой сервер.

Пример: После успешного CI-пайплайна: GitLab CD-задача собирает Docker-образ → загружает на Docker Hub → SSH-подключается к тестовому серверу → выполняет docker pull и docker run, останавливая старый контейнер → приложение доступно по адресу тестового сервера.
    """,

    """
🎫 Билет 20

1. Менеджеры пакетов: npm (Node.js/JS), pip (Python), Composer (PHP), Maven/Gradle (Java), NuGet (.NET), Cargo (Rust) --- автоматизируют загрузку, установку и обновление библиотек.

2. Роль: фиксация конкретных версий зависимостей (воспроизводимость); автоматическое разрешение транзитивных зависимостей; обновление библиотек командой; удаление неиспользуемых пакетов.

Пример: Добавить библиотеку: pip install requests==2.31.0 (Python) → requirements.txt обновляется автоматически командой pip freeze > requirements.txt → git add requirements.txt → git commit -m "Добавлена библиотека requests" → git push. Теперь любой участник выполнит pip install -r requirements.txt и получит идентичное окружение.
    """,

    """
🎫 Билет 21

1. Генераторы документации: Javadoc (Java), Sphinx (Python), JSDoc (JavaScript), Doxygen (C++) --- создают HTML/PDF документацию из специальных комментариев в коде.

2. Форматы комментариев: Javadoc: /** @param, @return, @throws */; Python: docstring с разделами Args, Returns, Raises; JSDoc: /** @param {тип} имя описание */.

Пример: /** Вычисляет средний балл студента. @param grades список оценок (не пустой) @return среднее арифметическое @throws IllegalArgumentException если список пуст */ public double calcAverage(List<Integer> grades) { ... } → javadoc генерирует HTML-страницу с описанием метода, параметров и возвращаемого значения.
    """,

    """
🎫 Билет 22

1. Линтеры проверяют стиль и возможные ошибки (ESLint, Pylint, Checkstyle); форматтеры автоматически переформатируют код (Prettier, Black, Google Java Format).

2. Единый стиль в команде: снижает когнитивную нагрузку при ревью, упрощает onboarding новых участников, уменьшает «шум» в diff-просмотрах (только содержательные изменения, без споров об отступах).

Пример: Правила: 1) Отступ --- 4 пробела (не табуляция); 2) Длина строки --- не более 120 символов; 3) Имена переменных --- camelCase; 4) Константы --- UPPER_SNAKE_CASE; 5) Открывающая фигурная скобка --- на той же строке что и блок. Инструмент: Checkstyle читает rules.xml и подсвечивает нарушения в IDE и CI.
    """,

    """
🎫 Билет 23

1. Локальный репозиторий --- копия на компьютере разработчика; удалённый (remote) --- на сервере (GitHub, GitLab, Bitbucket). Синхронизация через git push/pull/fetch.

2. Форк --- личная копия чужого репозитория; pull-request (PR/MR) --- предложение слить изменения из форка/ветки в основной репозиторий. Позволяет проводить ревью до слияния.

Пример: 1) Fork репозитория преподавателя на GitHub → 2) git clone <url своего форка> → 3) создать ветку feature/task1 → 4) внести изменения, коммит → 5) git push origin feature/task1 → 6) на GitHub нажать «New Pull Request», выбрать ветку → 7) преподаватель проверяет и делает Merge или просит исправить.
    """,

    """
🎫 Билет 24

1. Логирование --- запись событий приложения в файл/систему (Log4j, Logback, Python logging); мониторинг --- визуализация метрик в реальном времени (Grafana, ELK Stack, Datadog).

2. Анализ логов: фильтрация по уровню (ERROR, WARN, INFO); поиск по временному диапазону; поиск по ключевому слову (например, «NullPointerException»); восстановление последовательности событий до ошибки.

Пример: Запись в журнале: [2024-04-10 14:32:01] ERROR StudentService - Не найден студент с id=42, пользователь: admin. Выводы: 14:32 --- время инцидента; StudentService.getById(42) --- выбросил исключение; admin --- возможно некорректный запрос; следует проверить: существует ли id=42 в БД, добавить проверку на null до запроса.
    """,

    """
🎫 Билет 25

1. Инструменты нагрузочного тестирования: Apache JMeter (HTTP, GUI и CLI), k6 (скрипты на JS), Gatling (Scala-сценарии), Locust (Python) --- имитируют множество одновременных пользователей.

2. Параметры нагрузочного тестирования: число виртуальных пользователей; интенсивность (запросов в секунду); длительность теста; время разгона (ramp-up); целевые показатели: время отклика (p95, p99), пропускная способность, процент ошибок.

Пример: Сценарий JMeter для «Электронного журнала»: 50 виртуальных преподавателей; время разгона --- 60 сек; длительность --- 5 минут; операции: авторизация (10%), просмотр журнала группы (50%), выставление оценки (30%), выход (10%); критерий успеха: p95 < 2 сек, ошибок < 1%.
    """,

    """
🎫 Билет 26

1. Инструменты безопасности: OWASP ZAP и Burp Suite (сканеры веб-уязвимостей); OWASP Dependency-Check, Snyk, npm audit --- проверяют зависимости на известные CVE-уязвимости; SonarQube (обнаружение security hotspots в коде).

2. Проверка зависимостей: инструмент сравнивает список библиотек с базой CVE; выдаёт отчёт: библиотека, версия, номер уязвимости, критичность (Low/Medium/High/Critical), рекомендованная версия.

Пример: План проверки: 1) npm audit / pip-audit --- выявить уязвимые зависимости → обновить; 2) SonarQube --- статический анализ кода на security hotspots (SQL-инъекции, XSS); 3) OWASP ZAP --- автоматическое сканирование веб-интерфейса; 4) Ручная проверка авторизации --- тест на доступ без прав; 5) Составить отчёт и устранить Critical и High.
    """,

    """
🎫 Билет 27

1. UI-дизайнеры (визуальные конструкторы): Figma (веб-прототипирование), Qt Designer (Qt/C++), Glade (GTK), WindowsForms Designer (Visual Studio), JavaFX Scene Builder --- позволяют создавать интерфейс перетаскиванием элементов.

2. Преимущества визуального конструирования: мгновенный предварительный просмотр; нет необходимости вручную прописывать координаты и размеры; возможность показать макет заказчику до написания кода; ускорение разработки типовых форм.

Пример: В JavaFX Scene Builder: перетащить VBox (вертикальный контейнер) → добавить TextField с меткой «Имя», TextField «Email», PasswordField «Пароль», Button «Зарегистрироваться» → настроить выравнивание и отступы → сохранить как registration.fxml → IDE (IntelliJ) подключает файл: FXMLLoader.load("registration.fxml"), привязывает поля к контроллеру через @FXML.
    """,

    """
🎫 Билет 28

1. Генераторы проектов (скелетоны): Spring Initializr (Java/Spring Boot), Create React App (React), Vue CLI, Django startproject (Python), dotnet new (C#) --- создают базовую структуру проекта с нужными зависимостями и конфигурацией.

2. Преимущества: стандартная структура с первого дня; готовая конфигурация сборки; подключены базовые зависимости; отсутствие ошибок «чистого листа»; команда сразу пишет код, а не настраивает окружение.

Пример: Spring Initializr (start.spring.io): 1) выбрать Gradle/Maven, Java, версию Spring Boot; 2) задать Group: ru.college, Artifact: gradebook; 3) добавить зависимости: Spring Web, Spring Data JPA, PostgreSQL Driver; 4) нажать Generate → скачать .zip → распаковать → открыть в IntelliJ IDEA → gradle build --- проект сразу собирается.
    """,

    """
🎫 Билет 29

1. Плагины IDE --- дополнительные модули, расширяющие возможности среды разработки: поддержка языков, интеграции, визуализации, утилиты.

2. Критерии выбора: соответствие используемому языку/фреймворку; активная поддержка и регулярные обновления; высокий рейтинг в Marketplace; не снижает производительность IDE; наличие документации.

Пример: Полезные плагины (VS Code): 1) GitLens --- история изменений каждой строки кода прямо в редакторе; 2) Prettier --- автоформатирование при сохранении; 3) REST Client --- отправка HTTP-запросов прямо из .http-файлов; 4) Docker --- управление контейнерами из IDE; 5) SonarLint --- подсветка нарушений качества кода в реальном времени.
    """,

    """
🎫 Билет 30

1. Инструменты работы с API: Postman (GUI-клиент, коллекции запросов, тесты), Insomnia, REST Client (плагин VS Code), curl (консольный), Swagger UI (тестирование прямо из документации).

2. JSON --- лёгкий текстовый формат (пары ключ-значение, массивы); XML --- тегово-атрибутивный формат, более многословный. Postman/Insomnia автоматически форматируют и подсвечивают оба формата.

Пример: Тестирование POST /api/students: в Postman выбрать метод POST, ввести URL, перейти на вкладку Body → raw → JSON, вставить {"name":"Иванов","group":1}; добавить заголовок Content-Type: application/json; нажать Send; проверить: статус 201 Created, в теле ответа --- id нового студента; добавить тест: pm.test("Status 201", () => pm.response.to.have.status(201)).
    """,

    """
🎫 Билет 31

1. Контейнеризация (Docker) --- упаковка приложения и всех его зависимостей в изолированный контейнер, работающий одинаково на любом хосте.

2. Преимущества контейнеров: «работает на моей машине» --- исчезает; быстрый старт тестовой среды; изоляция сервисов; простой откат к предыдущей версии; воспроизводимость окружения в CI.

Пример: Dockerfile описывает образ приложения (базовый образ Java, копирование jar, команда запуска). docker-compose.yml объединяет два сервиса: app (наш jar) и db (postgres:15). Команда docker compose up поднимает оба контейнера: БД инициализируется → приложение подключается → доступно на localhost:8080.
    """,

    """
🎫 Билет 32

1. Резервное копирование артефактов: удалённый репозиторий Git (код, история); архивы релизов (GitLab/GitHub Releases); дампы БД; снапшоты виртуальных машин; хранение в облаке (S3, Google Drive).

2. Риски без резервного копирования: безвозвратная потеря кода при поломке диска; невозможность восстановить данные после ошибки; задержка разработки; потеря истории изменений.

Пример: План для учебного проекта: Код --- git push в GitLab ежедневно после каждой рабочей сессии; БД --- pg_dump еженедельно, хранить последние 4 дампа на облачном диске; Документация --- в репозитории вместе с кодом; Артефакты сборки --- jar-файлы тегированных релизов хранятся в GitLab Packages.
    """,

    """
🎫 Билет 33

1. Консольные инструменты: javac/java (компиляция и запуск Java), python (интерпретатор), gcc (C/C++), git (VCS), curl (HTTP-запросы), grep/find (поиск в файлах), ls/cd/cp/mv (навигация и работа с файлами).

2. Преимущества консоли наряду с IDE: работа на серверах без GUI; автоматизация через скрипты; скорость для опытного разработчика; незаменимы при отладке удалённых систем; понимание того, что IDE делает «под капотом».

Пример: Компиляция и запуск Java: cd src/main → javac Main.java -d out/ → java -cp out Main. Python: cd project → python -m venv venv → source venv/bin/activate → pip install -r requirements.txt → python main.py. Сборка Maven: mvn clean package → java -jar target/app.jar.
    """,

    """
🎫 Билет 34

1. Инструменты локализации: Java ResourceBundle (файлы .properties), GNU gettext (.po/.mo файлы), i18next (JavaScript), Babel (Python), Crowdin/Lokalise (платформы управления переводами).

2. Поддержка нескольких языков: все строки интерфейса выносятся в файлы ресурсов (messages_ru.properties, messages_en.properties); приложение при старте определяет язык пользователя и загружает нужный файл; переключение языка --- без перекомпиляции.

Пример: Java (Spring): создать messages_ru.properties (login.button=Войти) и messages_en.properties (login.button=Login); в конфиге указать basename=messages; в шаблоне (Thymeleaf): th:text="#{login.button}"; при запросе с Accept-Language: en --- выводится "Login", при ru --- "Войти". Смена языка доступна через параметр ?lang=en в URL.
    """,

    """
🎫 Билет 35

1. Генераторы тестовых данных: Faker (Python/JS/Java) --- генерирует реалистичные имена, адреса, даты; Mockaroo (онлайн) --- CSV/JSON с нужной схемой; DataFactory (Java); SQL-скрипты с генерацией INSERT.

2. Использование при тестировании: заполнение БД большим объёмом данных для нагрузочных тестов; воспроизведение граничных случаев; тестирование поиска и фильтрации на реалистичных данных без использования реальных персональных данных.

Пример: Python + Faker: создать скрипт, генерирующий 1000 записей --- случайные ФИО, даты рождения (1998--2006), group_id (1--10); сформировать SQL INSERT INTO students (name, birthdate, group_id) VALUES (...); выполнить скрипт: python generate.py | psql -d gradebook; проверить: SELECT COUNT(*) FROM students → 1000.
    """,

    """
🎫 Билет 36

1. Инструменты diff/merge: git diff (консоль), встроенный diff в IntelliJ IDEA и VS Code, WinMerge (Windows), Beyond Compare, Meld --- показывают различия между версиями файлов и позволяют выбирать нужные изменения.

2. Анализ различий: инструмент выделяет добавленные строки (зелёным), удалённые (красным), изменённые (жёлтым); показывает контекст вокруг изменений; при конфликтах --- три панели (ваша версия / общая база / другая версия).

Пример: В IntelliJ IDEA: щёлкнуть правой кнопкой на файле Student.java → Git → Show Diff; слева --- старая версия, справа --- текущая; видно: строка 15 --- изменено имя метода; строки 20--25 --- добавлена новая логика валидации; строка 30 --- удалён устаревший комментарий. Можно откатить отдельные изменения кнопкой «Revert Chunk».
    """,

    """
🎫 Билет 37

1. Инструменты для графических ресурсов: редакторы Figma, Inkscape (SVG), GIMP; в IDE --- Asset Manager (Android Studio), Image Assets (Xcode); плагины VS Code: SVG Preview, Image Preview.

2. Форматы: PNG --- растровое с прозрачностью (иконки); SVG --- векторное, масштабируемое (логотипы); JPEG --- фото; ICO/ICNS --- иконки приложений; WebP --- современный веб-формат с хорошим сжатием.

Пример: Добавление иконок в учебный веб-проект: 1) скачать иконки в SVG/PNG (Flaticon, Material Icons); 2) поместить в папку src/main/resources/static/icons/; 3) в HTML: <img src="/icons/search.svg" alt="Поиск">; для CSS: background-image: url('/icons/logo.png'); 4) зафиксировать в Git (git add src/main/resources/static/ → commit).
    """,

    """
🎫 Билет 38

1. Инструменты рефакторинга в IDE: встроенные в IntelliJ IDEA, Eclipse, VS Code --- позволяют безопасно переименовывать символы по всему проекту, извлекать методы, переносить классы, менять сигнатуры методов.

2. Автоматические операции рефакторинга: Rename (переименование с обновлением всех ссылок); Extract Method (выделение блока в отдельный метод); Inline (вставка тела метода на место вызова); Move (перенос класса в другой пакет); Change Signature (изменение параметров с исправлением всех вызовов).

Пример: Было: в методе main() 50 строк смешанной логики. В IntelliJ IDEA: выделить строки 10--20 (расчёт среднего балла) → ПКМ → Refactor → Extract Method → ввести имя calcAverage → OK. Результат: создан метод calcAverage(), на месте выделенного блока --- вызов. IDE автоматически определила параметры и возвращаемый тип. Код стал короче и понятнее.
    """,

    """
🎫 Билет 39

1. Инструменты документирования архитектуры: draw.io, PlantUML (диаграммы как код), Structurizr (C4-модель), Confluence (вики с диаграммами), Enterprise Architect --- для диаграмм развёртывания (Deployment Diagram UML).

2. Хранение и актуализация схем: хранить диаграммы в репозитории рядом с кодом (папка /docs/architecture); использовать «диаграммы как код» (PlantUML .puml файлы) --- изменения отслеживаются через git diff; обновлять при каждом архитектурном изменении.

Пример: В draw.io (diagrams.net): создать диаграмму развёртывания --- узлы: «Браузер студента», «Веб-сервер (Tomcat)», «Сервер БД (PostgreSQL)»; стрелки: HTTPS (443) от браузера к серверу, JDBC (5432) от сервера к БД; экспортировать в PNG и SVG → сохранить в /docs/deployment.drawio и /docs/deployment.png → git commit. Версия диаграммы всегда соответствует версии кода.
    """,

    """
🎫 Билет 40

1. Мок (mock) --- имитация объекта, проверяющая, как с ним взаимодействовали (Mockito, unittest.mock); стаб (stub) --- имитация, возвращающая заранее заданный ответ без проверки вызовов; WireMock --- заглушка HTTP-сервиса.

2. Необходимость имитаций: реальный внешний сервис может быть недоступен, платным или непредсказуемым; с моком тест стабилен, быстр и не зависит от сети; можно тестировать сценарии ошибок (таймаут, отказ оплаты).

Пример: WireMock для платёжного сервиса: запустить WireMock-сервер на localhost:8089; настроить заглушку: POST /api/pay → ответ 200 {"status":"success"}; в тесте указать URL платёжного сервиса = localhost:8089; вызвать метод оформления заказа; проверить: заказ создан, статус «Оплачен». Добавить сценарий ошибки: POST /api/pay → 503 → проверить корректную обработку отказа.
    """,

    """
🎫 Билет 41

1. Инструменты проверки лицензий: FOSSA, WhiteSource, TLDR Legal, license-checker (npm), pip-licenses (Python) --- анализируют зависимости проекта и их лицензии.

2. Риски несовместимых лицензий: GPL-библиотека в закрытом коммерческом продукте --- нарушение условий; некоторые лицензии требуют указания авторства или публикации исходников; судебные иски и принудительное открытие кода.

Пример: Для учебного Java-проекта: mvn license:aggregate-download-licenses → получить CSV-отчёт со всеми зависимостями и их лицензиями; проверить: все ли библиотеки MIT/Apache 2.0/BSD (совместимы с открытым учебным проектом); если найдена GPL --- заменить библиотеку на аналог с подходящей лицензией; добавить LICENSES.md в репозиторий.
    """,

    """
🎫 Билет 42

1. Инструменты коммуникации в разработке: Slack, Telegram, Microsoft Teams --- мессенджеры с ботами, получающими уведомления из CI/CD, трекеров задач и репозиториев.

2. Роль уведомлений о сборках: команда мгновенно узнаёт о сбое сборки и может оперативно исправить; уведомления о успешных PR-слияниях; напоминания о дедлайнах задач --- сокращают время реакции.

Пример: В GitLab: Settings → Integrations → Slack или Telegram Bot; указать Webhook-URL бота; выбрать события: Pipeline failed, Pipeline succeeded, Merge request created; теперь при каждом коммите в Telegram-чат команды приходит сообщение: «Сборка #42 --- FAILED. Ветка: feature/login. Ошибка: тест TestLogin упал». Команда реагирует без ручной проверки CI.
    """,

    """
🎫 Билет 43

1. Инструменты управления требованиями: Confluence (страницы-спецификации), Notion, GitLab/GitHub Wiki, ReqView, простые таблицы в Excel/Google Sheets --- хранят требования в структурированном виде.

2. Связь требований с задачами и тестами: каждое требование → одна или несколько задач в трекере; каждая задача → набор тест-кейсов; при изменении требования --- видно, какие задачи и тесты нужно обновить (трассируемость).

Пример: В Confluence: страница «Требования к журналу оценок»: таблица с колонками ID, Описание требования, Приоритет, Связанная задача (ссылка на Jira), Тест-кейс (ссылка на тест). REQ-01: «Преподаватель может выставить оценку» → задача PROJ-5 → TC-001, TC-002. При изменении оценочной шкалы --- открываем REQ-01 и видим все связанные артефакты для обновления.
    """,

    """
🎫 Билет 44

1. Инструменты миграции БД: Flyway (Java), Liquibase (Java), Alembic (Python/SQLAlchemy), Django Migrations --- управляют версионированными скриптами изменения схемы БД.

2. Преимущества миграций: изменения схемы версионируются в репозитории; автоматическое применение при деплое; возможность откатить изменения (rollback); вся команда работает с одинаковой версией схемы; нет расхождений между средами.

Пример: Flyway (Java): создать файл V3__add_email_to_students.sql в папке db/migration: ALTER TABLE students ADD COLUMN email VARCHAR(255); при следующем запуске приложения Flyway проверяет версию схемы → видит, что V3 не применена → выполняет ALTER TABLE → фиксирует в таблице flyway_schema_history. Откат (если поддерживается): создать U3__remove_email.sql.
    """,

    """
🎫 Билет 45

1. Интеграция инструментов: IDE (написание кода) → VCS/Git (контроль версий) → Issue-трекер (управление задачами) → CI/CD (автоматическая сборка и деплой) → мессенджер (уведомления команды) --- образуют единый процесс разработки.

2. Преимущества комплексной поддержки: прозрачность процесса; автоматизация рутинных операций; снижение числа ошибок из-за человеческого фактора; быстрая обратная связь; история всех изменений и решений доступна команде.

Пример: Цепочка учебного проекта: 1) Задача создана в GitLab Issues (#7 «Добавить поиск студентов»); 2) Разработчик в IntelliJ IDEA создаёт ветку feature/search-7; 3) Пишет код → коммит с упоминанием «#7»; 4) git push → GitLab CI запускает сборку и тесты; 5) Тесты прошли → уведомление в Telegram; 6) Создаётся Merge Request → ревью → Merge в main; 7) CD деплоит на тестовый сервер → Issues #7 закрывается автоматически.
    """,

]

assert len(TICKETS) == 45, len(TICKETS)

TICKET_TEXTS: dict[str, str] = {str(i + 1): TICKETS[i].strip() for i in range(45)}

